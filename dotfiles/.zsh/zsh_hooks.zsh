function precmd {
  ##############################################################################
  # antigen bundle rupa/z - https://github.com/rupa/z
  ##############################################################################
  # _z --add "$(pwd -P)"

  # if [[ -n "$TMUX" ]]; then
  #   tmux setenv "$(tmux display -p 'TMUX_PWD_#D')" "$PWD"
  # fi
}

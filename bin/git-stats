#!/usr/bin/env bash
#
#

usage() {
  echo "$@"
}

git_stats() {
  for d in "$(pwd)"/*; do
    if [ -d "$d/.git" ]; then
      echo
      echo "$d"
      (cd "$d" && git status)
    fi
  done
}

# _git_stats() {
#
# }

git_stats "$@"

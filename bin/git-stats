#!/usr/bin/env bash
#
#

usage() {
  echo "$@"
}

git_stats() {
  local res
  for d in "$(pwd)"/*; do
    if [ -d "$d/.git" ]; then
      echo
      echo $d
      (cd $d && git status)
    fi
  done
}

# _git_stats() {
#
# }

git_stats "$@"

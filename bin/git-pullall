#!/usr/bin/env bash
#
#

usage() {
  echo "$@"
}

git_pullall() {
  find . -type d -depth 1 -exec git --git-dir={}/.git --work-tree=$PWD/{} pull origin master \;
}

# _git_pullall() {
#
# }

git_pullall "$@"
